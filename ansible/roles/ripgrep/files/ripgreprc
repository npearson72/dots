# Search hidden files and directories
--hidden

# Follow symbolic links
--follow

# Don't respect ignore files (.gitignore, .ignore, etc.)
--no-ignore

# Don't print lines longer than this limit
--max-columns=10000

# Searches case insensitively
--smart-case

# Sort by file path
--sort=path

# Exclude .dot files
--glob=!{.DS_Store}

# Exclude .dot directories
--glob=!{.cache,.expo*,.git,.next,.svn,.tldr,.terraform,.turbo,.webpack}

# Exclude dev directories
--glob=!{**/node_modules,**/cache,**/debug,**/log,**/tmp}

# Exclude mobile dev directories
--glob=!{**/ios,**/android}

# Exclude build directories
--glob=!{**/dist,**/build,**/public/packs,**/public/vite-dev}

# Exclude dev files
--glob=!{pnpm-lock.yaml,package-lock.json,yarn-error*,yarn.lock}
