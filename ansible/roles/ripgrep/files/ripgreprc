# Search hidden files and directories
--hidden

# Follow symbolic links
--follow

# Don't respect ignore files (.gitignore, .ignore, etc.)
--no-ignore

# Don't print lines longer than this limit
--max-columns=10000

# Searches case insensitively
--smart-case

# Sort by file path
--sort=path

# Exclude .dot directories
--glob=!{.cache,.expo*,.git,.svn,.tldr}

# Exclude dev directories
--glob=!{dist*,log,node_modules,tmp}

# Exclude dev files
--glob=!{package-lock.json,yarn-error*,yarn.lock}
