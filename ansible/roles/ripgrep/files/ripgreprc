# Search hidden files and directories
--hidden

# Follow symbolic links
--follow

# Don't respect ignore files (.gitignore, .ignore, etc.)
--no-ignore

# Don't print lines longer than this limit
--max-columns=10000

# Searches case insensitively
--smart-case

# Sort by file path
--sort=path

# Exclude .dot directories
--glob=!{.git,.svn,.tldr}

# Exclude nodejs directories
--glob=!{node_modules}

# Exclude webpack directories
--glob=!{public/packs,dist*}

# Exclude rails directories
--glob=!{log/*.log,tmp}

# Exclude nodejs files
--glob=!{package-lock.json}

# Exclude yarn files
--glob=!{yarn-error*,yarn.lock}
